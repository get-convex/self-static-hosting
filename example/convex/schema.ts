import { defineSchema } from "convex/server";

// The static hosting component manages its own schema internally.
// You only need to define your app-specific tables here.
export default defineSchema({
  // Add your app-specific tables here
});
